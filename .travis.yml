language: python
dist: xenial
python:
  - 3.7
before_install:
  - sudo apt-get install -yy gcc
install:
  - pip install pip --upgrade
  - pip install poetry --upgrade
  - poetry install
script:
  - poetry run lint
  - poetry run test
after_success:
  - coveralls
